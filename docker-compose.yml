version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: hotel-billing-web:latest
    container_name: hotel-billing-web
    ports:
      - "8080:80"
    restart: unless-stopped
